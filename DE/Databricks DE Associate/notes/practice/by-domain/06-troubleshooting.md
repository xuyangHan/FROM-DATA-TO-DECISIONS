# Domain 6 — Troubleshooting (practice)

1. Job duration jumped from 12 min to 45 min over last week. First UI place to compare runs?

2. Task `transform` failed; `publish` shows Skipped. What do you fix first?

3. Spark stage shows 2 TB shuffle read and spill to disk. Two tuning knobs to try?

4. New table filtered heavily on `country` and `date`. Layout feature vs naive partition on `user_id`?

5. `df.collect()` on 10M rows caused driver OOM. Better pattern?

6. Cluster fails at startup after init script change. Where is evidence?
