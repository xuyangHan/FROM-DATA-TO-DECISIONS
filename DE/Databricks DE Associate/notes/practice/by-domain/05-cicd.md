# Domain 5 — CI/CD (practice)

1. Developer should not commit directly to `main`. Supported workflow in Repos?

2. `prod_main` vs `dev_main` catalog names across workspaces. Bundle feature?

3. Same job definition in dev and prod — what file packages resources?

4. Pre-merge CI should catch invalid YAML. CLI command?

5. Production job should run as service principal, not user. Bundle field name?
