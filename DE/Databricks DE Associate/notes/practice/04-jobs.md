# Domain 4 — Jobs (practice)

1. Task B should run only if Task A succeeds; Task C sends alert if A fails. Which job features?

2. Nightly SQL refresh of gold table — task type and compute?

3. Dashboard must refresh after gold SQL succeeds. Task order?

4. Bronze table receives unpredictable afternoon updates; silver should run after commit. Trigger type?

5. Job must run every day at 2 AM UTC regardless of new data. Trigger type?

6. Transient S3 read failures in extract task. Configuration to add?
